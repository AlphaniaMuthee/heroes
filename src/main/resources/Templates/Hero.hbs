{{#partial "content"}}
    <h1>These are the heroes we have</h1>
    {{#if Heroes}}
        {{#each Heroes}}
            <h2>{{hero.Name}}</h2>
            <p>Your superhero, {{hero.Name}}, is {{hero.Age}} years old. Their special power is {{hero.Proficiency}}. However, they have a weakness which is {{hero.Fragility}}</p>
        {{/each}}
    {{else}}
        <p>Oops! We can't seem to find a superhero.</p>
    {{/if}}
    <a href="/Squad/Heroes/form"><button>Create another hero</button></a>
{{/partial}}
{{> layout.hbs}}